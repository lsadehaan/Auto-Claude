# Auto-Claude Web Server Configuration
# Copy this file to .env and update the values

# =============================================================================
# Server Configuration
# =============================================================================

# Port to run the web server on
PORT=3001

# Host to bind to (0.0.0.0 for all interfaces, 127.0.0.1 for localhost only)
HOST=0.0.0.0

# =============================================================================
# Authentication
# =============================================================================

# Password hash for web interface login
# Generate this by running: npm run setup-password
AUTO_CLAUDE_PASSWORD_HASH=

# Session secret for cookie signing (change this to a random string)
SESSION_SECRET=change-me-to-a-random-string

# Session max age in milliseconds (default: 24 hours)
SESSION_MAX_AGE=86400000

# =============================================================================
# CORS Configuration
# =============================================================================

# Allowed origins for CORS (comma-separated for multiple)
# In development, all origins are allowed
CORS_ORIGIN=http://localhost:5173

# =============================================================================
# Claude Configuration (Required)
# =============================================================================

# OAuth token from Claude Code CLI
# Get this by running: claude setup-token
CLAUDE_CODE_OAUTH_TOKEN=

# =============================================================================
# Paths (Optional - defaults are usually fine)
# =============================================================================

# Path to the built frontend (default: ../frontend/dist-web)
# FRONTEND_DIST_PATH=

# Path to store server data (default: ./data)
# DATA_DIR=

# Path to Python executable (default: python)
# PYTHON_PATH=python

# Path to the Python backend (default: ../backend)
# BACKEND_PATH=

# =============================================================================
# Development
# =============================================================================

# Set to 'production' for production mode
NODE_ENV=development
